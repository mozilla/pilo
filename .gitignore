node_modules
dist
.env
.env.*
.DS_Store
*.txt
CLAUDE.md
scripts/docker-build.sh
debug

# Build artifacts
src/buildMode.ts.backup

# Lockfiles (project uses npm, others can use their preferred PM locally)
pnpm-lock.yaml
yarn.lock
bun.lockb
